openapi: 3.0.3
info:
  title: Mini DMS API
  version: 1.0.0
  description: Private document management with asynchronous extraction and recoverable
    jobs.
paths:
  /api/v1/admin/jobs/:
    get:
      operationId: api_v1_admin_jobs_list
      parameters:
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      tags:
      - api
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedJobList'
          description: ''
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '401':
          description: Authentication required or invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '404':
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '500':
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '503':
          description: Dependency unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
  /api/v1/admin/users/:
    get:
      operationId: api_v1_admin_users_list
      parameters:
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      tags:
      - api
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedUserList'
          description: ''
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '401':
          description: Authentication required or invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '404':
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '500':
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '503':
          description: Dependency unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
    post:
      operationId: api_v1_admin_users_create
      tags:
      - Accounts
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
          description: ''
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '401':
          description: Authentication required or invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '404':
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '500':
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '503':
          description: Dependency unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '409':
          description: State or idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '412':
          description: Revision conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '428':
          description: If-Match required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '413':
          description: Payload too large
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '415':
          description: Unsupported media type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
  /api/v1/audit-events/:
    get:
      operationId: api_v1_audit_events_list
      parameters:
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      tags:
      - api
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedAuditList'
          description: ''
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '401':
          description: Authentication required or invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '404':
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '500':
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '503':
          description: Dependency unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
  /api/v1/auth/logout/:
    post:
      operationId: api_v1_auth_logout_create
      description: |-
        Takes a token and blacklists it. Must be used with the
        `rest_framework_simplejwt.token_blacklist` app installed.
      tags:
      - api
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TokenBlacklistRequest'
        required: true
      security:
      - jwtAuth: []
      responses:
        '200':
          description: No response body
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '401':
          description: Authentication required or invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '404':
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '500':
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '503':
          description: Dependency unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '409':
          description: State or idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '412':
          description: Revision conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '428':
          description: If-Match required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '413':
          description: Payload too large
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '415':
          description: Unsupported media type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
  /api/v1/auth/me/:
    get:
      operationId: api_v1_auth_me_retrieve
      tags:
      - Accounts
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
          description: ''
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '401':
          description: Authentication required or invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '404':
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '500':
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '503':
          description: Dependency unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
  /api/v1/auth/token/:
    post:
      operationId: api_v1_auth_token_create
      description: |-
        Takes a set of user credentials and returns an access and refresh JSON web
        token pair to prove the authentication of those credentials.
      tags:
      - api
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TokenObtainPairRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenObtainPair'
          description: ''
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '401':
          description: Authentication required or invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '404':
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '500':
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '503':
          description: Dependency unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '409':
          description: State or idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '412':
          description: Revision conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '428':
          description: If-Match required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '413':
          description: Payload too large
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '415':
          description: Unsupported media type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
  /api/v1/auth/token/refresh/:
    post:
      operationId: api_v1_auth_token_refresh_create
      description: |-
        Takes a refresh type JSON web token and returns an access type JSON web
        token if the refresh token is valid.
      tags:
      - api
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TokenRefreshRequest'
        required: true
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TokenRefresh'
          description: ''
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '401':
          description: Authentication required or invalid
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '404':
          description: Resource not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '429':
          description: Rate limit exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '500':
          description: Unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '503':
          description: Dependency unavailable
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '409':
          description: State or idempotency conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '412':
          description: Revision conflict
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '428':
          description: If-Match required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '413':
          description: Payload too large
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '415':
          description: Unsupported media type
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorEnvelope'
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
  /api/v1/documents/:
    get:
      operationId: api_v1_documents_list
      parameters:
      - in: query
        name: document_type
        schema:
          type: string
      - name: page
        required: false
        in: query
        description: A page number within the paginated result set.
        schema:
          type: integer
      - name: page_size
        required: false
        in: query
        description: Number of results to return per page.
        schema:
          type: integer
      - in: query
        name: q
        schema:
          type: string
        description: Token-based full-text query, up to 200 characters.
      - in: query
        name: status
        schema:
          type: string
          enum:
          - failed
          - pending
          - processing
          - succeeded
      - in: query
        name: tag
        schema:
          type: string
        description: Exact, case-sensitive tag filter.
      tags:
      - Documents
      security:
      - jwtAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaginatedDocumentListList'
          description: ''
          headers:
            X-Request-ID:
              schema:
                type: string
                format: uuid
              description: Correlation ID shared with audit and job logs.
        '400':
          description: Invalid request
